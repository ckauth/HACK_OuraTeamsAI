const config = {
  botId: process.env.BOT_ID,
  botPassword: process.env.BOT_PASSWORD,
  openAIKey: process.env.SECRET_OPENAI_API_KEY,
  ouraKey: process.env.SECRET_OURA_API_KEY
};

export default config;
